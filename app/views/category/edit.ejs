<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Category - <%= category.categoryName %></title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <div class="login-form">
    <h2>Edit Category</h2>

    <% if (typeof status !== 'undefined') { %>
        <%if (status == 'success') { %>
            <div style="color: green;">
                Updated Successfully
            </div>
        <% } else { %>
            <div style="color: red;">
                Some error occurred while editing the Todo
            </div>
        <% } %>
        <br>
    <% } %>
    <form action="/category/<%= category.id %>" method="post">
        <input type="hidden" name="_method" value="put">
        <label for="categoryName">categoryName</label>
        <input type="text" id="categoryName" name="categoryName" value="<%= category.categoryName %>"
            <% if (typeof categoryName != "undefined" && categoryName) { %> value="<%= categoryName %>"<% } %>required>
        <% if (typeof conflictError != "undefined" && conflictError) { %>
            <div class="text-danger text-left  mb-3"><%= conflictError %></div>
        <% } %>    
        <br> <br>
        <button class="button button1" type="submit">Update</button>
        <a href="/category"><button class="button button1" type="button">Back</button></a>
    </form>
    </div>
</body>
</html>