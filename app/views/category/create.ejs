<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tạo danh mục</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <div class="login-form">
        <h2>Tạo danh mục</h2>
        <% if (typeof status !== 'undefined') { %>
            <%if (status == 'success') { %>
                <div style="color: green;">
                    Tạo danh mục thành công
                </div>
            <% } else { %>
                <div style="color: red;">
                    Có lỗi xảy ra trong quá trình tạo mới danh mục
                </div>
            <% } %>
            <br>
        <% } %>
        <form action="/category" method="POST">
            <label for="categoryName">Tên danh mục:</label>
            <input type="text" id="categoryName" name="categoryName" placeholder="Nhập tên danh mục"
                <% if (typeof categoryName != "undefined" && categoryName) { %> value="<%= categoryName %>"<% } %> required>
            <% if (typeof conflictError != "undefined" && conflictError) { %>
                <div class="text-danger text-left  mb-3"><%= conflictError %></div>
            <% } %>    
            <br> <br>
            <button class="button button1" type="submit">Tạo</button>
            <a href="/category"><button class="button button1" type="button">Trở về</button></a>
        </form>
    </div>
</body>
</html>